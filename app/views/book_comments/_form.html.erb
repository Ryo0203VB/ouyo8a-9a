 <%= form_with model: [@book,@book_comment], local: false, method: :post, url: book_book_comments_path(book_id: @book.id) do |f| %>
          <%= f.text_area :comment, rows:'5', placeholder: "コメントをここに", class: "w-100", id: "comment_textarea" %><br>
          <%= f.submit "送信" %>
        <% end %>